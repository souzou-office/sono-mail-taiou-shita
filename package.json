{
  "name": "sono-mail-taiou-shita",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "concurrently -n front,back -c cyan,green \"vite\" \"cd backend && npx tsx watch src/server.ts\"",
    "dev:front": "vite",
    "dev:back": "cd backend && npx tsx watch src/server.ts",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "vite build && gh-pages -d dist"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.3.0",
    "concurrently": "^9.2.1",
    "gh-pages": "^6.1.1",
    "vite": "^5.4.0",
    "vite-plugin-pwa": "^0.20.0"
  }
}
